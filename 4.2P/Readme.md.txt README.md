# SIT725 4.2P - Inventory with DB (SQLite) and Server-side functions

## Run locally
1. npm install
2. npm run init-db
3. npm start
4. Open http://localhost:3000

## API endpoints
- GET /api/items
- GET /api/items/:id
- POST /api/items
- PUT /api/items/:id
- DELETE /api/items/:id